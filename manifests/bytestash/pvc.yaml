apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: snippets
  namespace: bytestash
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 4Gi
  storageClassName: grouse-non-raid
status:
