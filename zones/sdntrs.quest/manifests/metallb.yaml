apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: sdntrs-quest
  namespace: metallb
spec:
  autoAssign: true
  avoidBuggyIPs: true
  addresses:
    - 10.4.20.0/24
---
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: sdntrs-quest
  namespace: metallb
spec:
  ipAddressPools:
    - sdntrs-quest
---
apiVersion: v1
kind: Service
metadata:
  name: dragonfruit
  namespace: sdntrs-quest
  annotations:
    metallb.universe.tf/address-pool: sdntrs-quest
    external-dns.alpha.kubernetes.io/hostname: dragonfruit.sdntrs.quest
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 5678
  selector:
    app: dragonfruit
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: dragonfruit
  name: dragonfruit
  namespace: sdntrs-quest
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dragonfruit
  template:
    metadata:
      labels:
        app: dragonfruit
    spec:
      containers:
      - image: hashicorp/http-echo
        name: http-echo
        args: ["-text", "dragonfruit"]
        ports:
          - containerPort: 5678
